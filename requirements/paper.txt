# Historical versions used for the paper experiments. Use this file only in an
# isolated reproduction environment with trusted model and dataset artifacts.
# For ordinary use, install the package through pyproject.toml instead.
torch==2.11.0
transformers==5.10.1

# LongBench evaluation dependencies.
numpy>=1.24
datasets
fuzzywuzzy
jieba
rouge
tqdm
